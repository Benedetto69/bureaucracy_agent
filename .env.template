# Copy this file to `.env` and replace placeholder values with production credentials.
# Do NOT commit your `.env` file to the repository.

API_BASE_URL=https://api.tuodominio.com/v1
BACKEND_API_TOKEN=replace-with-token
